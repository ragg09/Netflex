$(document).ready(function() {
	//I used .click function instead of
	//loading the file itself
	//becuase in the login page, you can still
	//choose sign in or sign up
	$(".navItemSignIn").click(); 
});